{
  "name": "frontend-e2e",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "frontend-e2e/src",
  "implicitDependencies": ["frontend"],
  "// targets": "to see all targets run: nx show project frontend-e2e --web",
  "targets": {
    "lint": {
  "command": "eslint frontend-e2e/src/**/*.ts --ignore-pattern dist --ignore-pattern build --ignore-pattern out --ignore-pattern test-output"
    }
  }
}
